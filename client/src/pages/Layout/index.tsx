import './style.scss'

import RoutesApp from "~/routes";

const Layout = () => {
    return (
        <div className={'layout'}>
            <RoutesApp/>
        </div>
    )
}

export default Layout;